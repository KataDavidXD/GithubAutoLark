# Copy this file to ".env" and fill values.
# Never commit ".env". This repo's .gitignore already ignores it.

# =============================================================================
# GitHub
# =============================================================================
# Required: GitHub PAT with issue permissions for the repo
GITHUB_TOKEN=

# Target repo (optional for demos; defaults may be used by scripts)
OWNER=KataDavidXD
REPO=GithubAutoLark

# =============================================================================
# Lark MCP (for Python scripts that spawn the MCP server)
# =============================================================================
# If you already have Cursor MCP configured, you can still set these for CLI demos.
LARK_MCP_CLIENT_ID=
LARK_MCP_CLIENT_SECRET=
LARK_MCP_DOMAIN=https://open.larksuite.com/
LARK_MCP_USE_OAUTH=true

# These are created by the Lark lifecycle demo and then persisted into .env
LARK_APP_TOKEN=
LARK_TASKS_TABLE_ID=

# Optional: notification destination for drift alerts (chat_id)
LARK_NOTIFY_CHAT_ID=

# =============================================================================
# Task table field names (must match Bitable exactly)
# =============================================================================
LARK_FIELD_TITLE=Task Name
LARK_FIELD_STATUS=Status
LARK_FIELD_ASSIGNEE=Assignee
LARK_FIELD_GITHUB_ISSUE=GitHub Issue
LARK_FIELD_LAST_SYNC=Last Sync

# =============================================================================
# Employee identity (used for assignee resolution demo)
# =============================================================================
EMPLOYEE_EMAIL=

# =============================================================================
# LLM (required for parsing fuzzy markdown docs into structured todos)
# =============================================================================
# OpenAI-compatible API key (OpenAI, Groq, Together, etc.)
LLM_API_KEY=

# Optional: custom base URL for OpenAI-compatible endpoints
# Examples:
#   https://api.openai.com/v1 (default)
#   https://api.groq.com/openai/v1
#   https://api.together.xyz/v1
LLM_BASE_URL=

# Optional: model name (default: gpt-4o-mini)
# Examples: gpt-4o, gpt-4o-mini, llama-3.1-70b-versatile
DEFAULT_LLM=

