# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# LangGraph agent (supervisor pattern)
langgraph>=0.2.0
langchain-core>=0.3.0

# Optional: LLM-based command parsing (future)
langchain-openai>=0.2.0

# Testing
pytest>=8.0.0
